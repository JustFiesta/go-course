# Excercise with Concurrency

Example API that is beeing hit outpus "DONE" when rate is exactly 5 requests in given second.

Objective was to create simple code to hit every time exactly 5 times per second hardoced endpoint.
